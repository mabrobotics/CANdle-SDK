@PACKAGE_INIT@
include(CMakeFindDependencyMacro)

find_dependency(Threads)

if(UNIX)
    find_dependency(PkgConfig)
    pkg_check_modules(LIBUSB REQUIRED IMPORTED_TARGET libusb-1.0)
endif()

include("${CMAKE_CURRENT_LIST_DIR}/candleTargets.cmake")