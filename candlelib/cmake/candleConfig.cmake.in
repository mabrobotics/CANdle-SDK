@PACKAGE_INIT@

include(CMakeFindDependencyMacro)
if(UNIX)
    list(APPEND CMAKE_MODULE_PATH "${CMAKE_CURRENT_LIST_DIR}")
    find_dependency(libusb REQUIRED)
endif()

include("${CMAKE_CURRENT_LIST_DIR}/candleTargets.cmake")

check_required_components(candle)
